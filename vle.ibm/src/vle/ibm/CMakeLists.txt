INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/src ${VLE_INCLUDE_DIRS}
  ${GVLE_INCLUDE_DIRS} ${Boost_INCLUDE_DIRS}
  ${GTKSOURCEVIEWMM_INCLUDE_DIRS} ${GTKMM_INCLUDE_DIRS}
  ${MUPARSER_INCLUDE_DIRS} ${LUA_INCLUDE_DIRS}
  ${CMAKE_SOURCE_DIR}/src)

LINK_DIRECTORIES(${VLE_LIBRARY_DIRS} ${GVLE_LIBRARY_DIRS}
  ${GTKSOURCEVIEWMM_LIBRARY_DIRS} ${GTKMM_LIBRARY_DIRS}
  ${MUPARSER_LIBRARY_DIRS} ${LUA_LIBRARY_DIRS}
  ${CMAKE_SOURCE_DIR}/lib)
  
include_directories(${CMAKE_SOURCE_DIR}/src ${VLE_INCLUDE_DIRS}
  ${LUA_INCLUDE_DIR})

link_directories(${VLE_LIBRARY_DIRS} ${CMAKE_SOURCE_DIR}/lib)

ADD_LIBRARY(Controleur SHARED Controleur.cpp ControleurProxy.cpp lunar.h)

TARGET_LINK_LIBRARIES(Controleur ${VLE_LIBRARIES}
  ${GVLE_LIBRARIES} ${GTKMM_LIBRARIES} ${LUA_LIBRARIES} mas)

INSTALL(TARGETS Controleur RUNTIME DESTINATION 
  plugins/simulator LIBRARY DESTINATION plugins/simulator)
