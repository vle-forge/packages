INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/src ${VLE_INCLUDE_DIRS}
  ${GVLE_INCLUDE_DIRS} ${Boost_INCLUDE_DIRS}
  ${GTKSOURCEVIEWMM_INCLUDE_DIRS})

LINK_DIRECTORIES(${VLE_LIBRARY_DIRS} ${GVLE_LIBRARY_DIRS}
  ${GTKSOURCEVIEWMM_LIBRARY_DIRS})

ADD_LIBRARY(output-file SHARED File.hpp File.cpp)
SET_TARGET_PROPERTIES(output-file PROPERTIES OUTPUT_NAME "file")

TARGET_LINK_LIBRARIES(output-file ${VLE_LIBRARIES} ${GVLE_LIBRARIES}
  ${GTKSOURCEVIEWMM_LIBRARIES})

INSTALL(TARGETS output-file
  RUNTIME DESTINATION plugins/gvle/output
  LIBRARY DESTINATION plugins/gvle/output)

INSTALL(FILES file.glade DESTINATION plugins/gvle/output)
