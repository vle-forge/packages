INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/src ${VLE_INCLUDE_DIRS}
  ${GVLE_INCLUDE_DIRS} ${Boost_INCLUDE_DIRS}
  ${GTKSOURCEVIEW_INCLUDE_DIRS})

LINK_DIRECTORIES(${VLE_LIBRARY_DIRS} ${GVLE_LIBRARY_DIRS}
  ${GTKSOURCEVIEW_LIBRARY_DIRS})

ADD_LIBRARY(storage-file SHARED Storage.cpp Storage.hpp)
SET_TARGET_PROPERTIES(storage-file PROPERTIES OUTPUT_NAME "storage")

TARGET_LINK_LIBRARIES(storage-file ${VLE_LIBRARIES} ${GVLE_LIBRARIES}
 ${GTKSOURCEVIEW_LIBRARIES})

INSTALL(TARGETS storage-file
  RUNTIME DESTINATION plugins/gvle/output
  LIBRARY DESTINATION plugins/gvle/output)

INSTALL(FILES storage.glade DESTINATION plugins/gvle/output)
